<script setup lang="ts">import GlIcon from "./GlIcon.vue";import GlStatusBadge from "./GlStatusBadge.vue";defineProps<{path:string;status?:string;statusLabel?:string;title?:string;sticky?:boolean}>();</script>
<template><header class="gl-diff-header" :class="{ 'is-sticky':sticky }"><div class="identity"><GlIcon name="file" /><GlStatusBadge v-if="status" :status="status">{{ statusLabel ?? status }}</GlStatusBadge><strong class="path" :title="title ?? path">{{ path }}</strong></div><div v-if="$slots.meta" class="meta"><slot name="meta" /></div><div v-if="$slots.actions" class="actions"><slot name="actions" /></div></header></template>
<style scoped>.gl-diff-header { min-width:0; min-height:48px; display:grid; grid-template-columns:minmax(0,1fr) auto; gap:var(--gl-spacing-4) var(--gl-spacing-12); align-items:center; padding:var(--gl-spacing-8) var(--gl-spacing-12); border-bottom:1px solid var(--gl-border-default); background:var(--gl-surface-raised); }.is-sticky { position:sticky; top:0; z-index:10; }.identity { min-width:0; display:flex; align-items:center; gap:var(--gl-spacing-8); }.path { min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; font-family:var(--vscode-editor-font-family); font-size:12px; }.meta { min-width:0; grid-column:1; color:var(--gl-text-subtle); font-size:10px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }.actions { grid-column:2; grid-row:1 / span 2; display:flex; align-items:center; gap:var(--gl-spacing-4); }</style>
