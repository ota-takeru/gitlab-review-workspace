<script setup lang="ts">import GlAvatar from "./GlAvatar.vue";defineProps<{author:string;avatarUrl?:string;date?:string;edited?:boolean;pending?:boolean}>();</script>
<template><article class="gl-comment" :class="{ 'is-pending':pending }"><GlAvatar :name="author" :avatar-url="avatarUrl" /><div class="content"><header><strong>{{ author }}</strong><span v-if="date">{{ date }}</span><em v-if="edited">edited</em><slot name="meta" /><span class="actions"><slot name="actions" /></span></header><div class="body"><slot /></div></div></article></template>
<style scoped>.gl-comment { display:grid; grid-template-columns:auto minmax(0,1fr); gap:var(--gl-spacing-8); padding:var(--gl-spacing-8) var(--gl-spacing-12); border-bottom:1px solid var(--gl-border-subtle); }.is-pending { opacity:var(--gl-disabled-opacity); }.content { min-width:0; display:grid; gap:var(--gl-spacing-4); }.content header { min-width:0; display:flex; align-items:center; gap:var(--gl-spacing-8); color:var(--gl-text-subtle); font-size:11px; }.content strong { color:var(--gl-text-strong); }.content em { font-size:10px; }.actions { margin-left:auto; }.body { line-height:1.5; overflow-wrap:anywhere; white-space:pre-wrap; }</style>
