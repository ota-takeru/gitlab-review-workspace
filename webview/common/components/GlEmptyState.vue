<script setup lang="ts">import GlIcon, { type GlIconName } from "./GlIcon.vue";withDefaults(defineProps<{ title: string; description?: string; icon?: GlIconName; compact?: boolean }>(), { icon:"information" });</script>
<template><div class="gl-empty-state" :class="{ 'is-compact':compact }"><GlIcon :class="{ spin: icon === 'spinner' }" :name="icon" :size="24" /><strong>{{ title }}</strong><p v-if="description">{{ description }}</p><div v-if="$slots.actions" class="actions"><slot name="actions" /></div></div></template>
<style scoped>.gl-empty-state { min-height:160px; display:grid; place-items:center; place-content:center; gap:var(--gl-spacing-8); padding:var(--gl-spacing-24); color:var(--gl-text-subtle); text-align:center; }.gl-empty-state strong { color:var(--gl-text-strong); }.gl-empty-state p { max-width:48ch; margin:0; line-height:1.45; }.is-compact { min-height:80px; padding:var(--gl-spacing-16); }.actions { display:flex; justify-content:center; gap:var(--gl-spacing-8); }</style>
